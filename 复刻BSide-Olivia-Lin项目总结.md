# 项目总结：复刻《BSide: Olivia Lin》AI 桌面陪伴软件（练手项目）

> 给新项目 Codex 的启动说明：请先完整阅读本文件，再按「六、路线图」从 M0/M1 开始搭建。目标是一个可运行的 MVP，不要一上来做完整商业化功能。

## 一、项目背景

- 参考产品：米哈游旗下 AI 桌面陪伴软件《BSide: Olivia Lin》（林离：在你身边）。
- 产品定位：不是传统游戏，而是以「AI 虚拟陪伴」为核心的桌面软件，2026-07-13 在 Steam 开启抢先体验，2026-08-11 宣布停运，2026-08-31 关闭服务器并下架。
- 角色设定：林离，上海大学生，钢琴专业，热爱黑胶唱片、老电影和雨天。
- 主要功能：AI 对话、书信往来、上传 MIDI 生成音乐影像、钢琴演奏、桌面动态壁纸。
- 本项目目标：做同类练手项目，复刻其"灵魂"（一个像真人的 AI 陪伴角色），不追求商业化和完整功能。

## 二、功能模块与优先级

| 优先级 | 模块 | 说明 |
| --- | --- | --- |
| P0 | 角色聊天 | 带人设的对话，是产品的核心 |
| P0 | 本地历史记录 | 聊天记录本地保存，重启可续聊 |
| P1 | 长期记忆 | 记住用户重要信息，跨会话使用 |
| P1 | 活人感细节 | 时间段问候、打字动画、主动消息等 |
| P2 | 语音 | TTS 朗读回复、语音输入 |
| P2 | 音乐 | 内置钢琴曲播放、MIDI 上传与可视化 |
| P3 | 桌面感 | 透明悬浮窗、系统托盘、动态壁纸 |
| P3 | 书信系统 | 异步写信-回信交互 |
| P4 | 账号/云端 | 可全部砍掉，保持纯本地 |

## 三、整体架构组成

1. **角色与人设系统**：角色卡（背景、性格、说话风格、称呼习惯）+ 长期记忆机制，保证角色一致性。
2. **AI 对话与生成引擎**：接入大模型 API（OpenAI 兼容接口），通过提示词/角色卡控制人格与语气，负责聊天、回信、创意建议。
3. **语音与音乐模块**：TTS 语音合成；钢琴演奏内容（预制曲库或程序生成）。
4. **MIDI 处理与可视化**：解析用户上传的 MIDI 文件（音符、节奏），驱动钢琴动画或生成音乐影像。
5. **书信交互系统**：异步消息机制（写信→排队→回信），配合邮件风格 UI。
6. **桌面客户端**：常驻窗口、动态壁纸、悬浮交互、文件上传。
7. **后端与数据**：练手阶段不需要，全部本地 JSON/SQLite。
8. **成本与部署**：控制模型调用成本是长期运营关键（练手阶段影响小）。

## 四、推荐技术栈

- 桌面壳：**Electron**（上手快、生态全；Tauri 更轻量但有 Rust 门槛）。
- 前端：React + Vite + Tailwind（可选）。
- AI：任一 OpenAI 兼容 API（DeepSeek / Kimi / 通义 / 本地 Ollama 均可），先不微调。
- 数据：本地 JSON 文件或 SQLite。
- 语音：TTS API（如 Edge TTS / 各家语音服务）；语音输入用 Web Speech API 或 Whisper。
- MIDI：JavaScript MIDI 解析库（如 tone.js / midi 解析器）+ 自绘钢琴动画。
- 密钥管理：API Key 放 `.env`，加入 `.gitignore`，绝不硬编码。

## 五、关键约束与注意事项

- **成本控制**：选便宜模型；上下文只保留最近约 20 轮 + 记忆摘要，防止账单失控。
- **纯本地优先**：不做账号、不做云同步；用户数据（聊天记录、信件、MIDI）保存在本地。
- **范围控制**：动态壁纸等平台相关功能放最后，甚至可以砍掉；MVP 先保证"能聊天"。
- **隐私与安全**：注意内容安全，本地数据避免明文存储敏感信息。
- **不要往无关目录散落文件**：项目文件只放在项目根目录内。

## 六、路线图（分阶段实施）

- **M0 骨架**：初始化 Electron + React + Vite 项目，能启动一个空窗口。
- **M1 角色聊天 MVP（核心）**：聊天界面 + 角色卡 + 调用大模型 + 本地保存历史。这一步完成后产品"灵魂"已经可用。
- **M2 记忆**：对话后提炼用户重要信息存到本地，下次对话注入上下文。
- **M3 活人感**：时间段问候、打字省略动画、随机延迟、偶尔主动发消息。
- **M4 语音**：TTS 朗读回复；语音输入。
- **M5 音乐**：内置钢琴曲；MIDI 上传解析与可视化。
- **M6 桌面感**：透明置顶悬浮窗、系统托盘、开机自启（壁纸最后做）。
- **M7 书信系统**：异步写信-回信 UI（可选）。

## 七、当前状态

- 尚未创建任何代码文件，项目从零开始。
- 下一步：在新项目目录中初始化 M0 骨架，实现 M1 角色聊天 MVP。
- 每完成一个里程碑，先跑通验证，再进入下一阶段，不要一次性铺开全部功能。
