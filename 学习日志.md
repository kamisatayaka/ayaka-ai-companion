# 学习日志：从零复刻「BSide: Olivia Lin」AI 桌面陪伴软件

> 这份日志记录本项目的每一步做了什么、为什么要这么做，以及**如果你想独立学会这部分内容，应该怎么学**。
> 建议用法：先跟着日志把项目跑起来，再按「独立学习路径」里的顺序补基础、做小练习。

---

## 日志说明

每一步包含三块内容：

- **做了什么**：这一步完成的事和涉及的代码文件。
- **为什么**：背后的设计考虑（尤其是踩过的坑）。
- **独立学习路径**：你想自己掌握这部分，该看什么、练什么。

建议学习顺序：**React 基础 → 前端状态 → Vite 构建 → Electron 主进程/IPC → 大模型 API**，和下面的步骤一一对应。

---

## Step 0：环境准备

**做了什么**

- 确认本机有 Node.js（v20.10.0）和 npm（10.2.3）。
- 确认项目目录 `G:\AI-girlfriendplan` 是干净的（只有需求总结 md）。

**独立学习路径**

- 安装 Node.js：<https://nodejs.org>（建议 LTS 版本）。
- 学会在终端执行 `node -v`、`npm -v`，知道 npm 是 Node 的包管理器。
- 了解 `package.json` 的基本字段：`scripts`（命令脚本）、`dependencies`（运行依赖）、`devDependencies`（开发依赖）、`main`（程序入口）。

---

## Step 1（M0）：初始化 Electron + Vite + React 骨架

**做了什么**

- 创建了 `package.json`，声明了本项目需要的依赖和 `npm run dev / build / start` 三个脚本。
- 创建了 `vite.config.js`：配置 React 插件、开发端口 5173、构建输出到 `dist/`。
- 创建了 `index.html` 和 `src/main.jsx`：React 的挂载入口。
- 创建了 `electron/main.js`（Electron 主进程）和 `electron/preload.cjs`（预加载脚本）。
- 运行 `npm install` 安装依赖，`npm run build` 验证前端能构建。

**为什么**

- 用 Vite 负责**前端**（React 界面）的开发体验：热更新、打包。
- 用 Electron 负责**桌面壳**：打开一个原生窗口，加载 Vite 构建出的页面。
- 开发时 Electron 加载 `http://localhost:5173`（热更新），生产时加载 `dist/index.html`。
- `main.js` 是 Node 环境，`src/` 里的 React 是浏览器环境，二者通过「预加载脚本 + IPC」通信（见 Step 4）。

**独立学习路径**

1. 看 Vite 官方起步教程（10 分钟）：<https://cn.vitejs.dev/guide/>
2. 自己跑一遍 `npm create vite@latest`，看它生成的项目结构和 `npm run dev` 效果。
3. 看 React 官方教程前几章，理解「组件」是什么：<https://zh-hans.react.dev/learn>
4. 小练习：自己创建一个 Vite + React 项目，改 `App.jsx` 里的文字，观察热更新。

---

## Step 2（M1）：角色卡（人设系统）

**做了什么**

- 创建了 `src/shared/character.js`：把神里绫华的背景、性格、说话风格、规则集中在一个对象里，并提供 `buildSystemPrompt()` 函数生成给大模型的 system prompt。
- 加入了时间段（凌晨/上午/下午/晚上）和星期几，让开场白更自然——这是「活人感」的第一个小细节。

**为什么**

- 角色一致性靠的是「每次对话都把角色卡注入系统提示词」，而不是让模型自由发挥。
- 角色卡集中管理：想改人设，只改这一个文件，不用动任何聊天逻辑。

**独立学习路径**

1. 理解「System Prompt / 角色卡」的概念：它是给大模型的「剧本设定」，模型在设定内扮演角色。搜「LLM system prompt role play」或看 OpenAI 文档里的 prompt 示例。
2. 小练习：把角色卡里的性格改成「高冷寡言」，看看回复风格变化；再改成「话痨」，对比差异。这是感受提示词工程最直观的方法。

---

## Step 3（M1）：聊天界面（React 组件）

**做了什么**

- `src/App.jsx`：聊天应用的顶层组件，管理「消息列表」「是否正在打字」「连接状态」三个状态。
- `src/components/ChatMessage.jsx`：单条消息气泡（区分用户/神里绫华、显示时间）。
- `src/components/MessageInput.jsx`：输入框，Enter 发送、Shift+Enter 换行。
- `src/styles.css`：暗色系 UI（紫调 + 夜晚氛围），头像、气泡、打字动画。
- 细节：中文输入法选字时按 Enter 不会误发送（`isComposing` 判断）；新消息自动滚到底部。

**为什么**

- React 的核心思想是「状态驱动界面」：`messages` 数组一变，界面自动重渲染。
- 组件拆分是为了可读性和复用：气泡和输入框各自独立，方便以后扩展（如换主题、加语音按钮）。

**独立学习路径**

1. React 官方教程重点学：State（`useState`）、Effects（`useEffect`）、组件和 Props：<https://zh-hans.react.dev/learn/state-a-component-s-memory>
2. 理解「受控组件」：输入框的值由 state 控制，而不是直接操作 DOM。
3. 小练习：给消息加「删除单条」按钮；给气泡加不同颜色主题切换。
4. CSS 推荐看 MDN 的 flexbox 教程：<https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Flexbox>

---

## Step 4（M1）：Electron 主进程、预加载与 IPC（本项目的核心难点）

**做了什么**

- `electron/main.js`：创建窗口、读取 `.env`、调用大模型、读写本地文件。
- `electron/preload.cjs`：用 `contextBridge` 把主进程能力安全地暴露给前端，形成 `window.api`。
- 前端通过 `window.api.sendChat(...)` / `loadHistory()` / `saveHistory()` 与主进程通信。

**为什么（这里是最容易理解错的地方）**

- Electron 应用分两层：
  - **主进程**（Node 环境）：有权限读写文件、访问网络、创建窗口。
  - **渲染进程**（浏览器环境）：显示界面，但没有 Node 权限（出于安全，`nodeIntegration` 已关闭）。
- 两层之间靠 **IPC（进程间通信）** 传消息：渲染进程 `invoke` 请求，主进程 `handle` 响应。
- API Key 放在主进程读取，前端代码里永远看不到 Key——这是安全底线。

**独立学习路径**

1. 看 Electron 官方文档的「进程模型」一章，这是最重要的概念：<https://www.electronjs.org/docs/latest/tutorial/process-model>
2. 看「上下文隔离（contextIsolation）」和「预加载脚本」：<https://www.electronjs.org/docs/latest/tutorial/tutorial-preload>
3. 小练习：在 preload 里新增一个 `window.api.ping()`，主进程返回 `pong`，前端点按钮显示结果。跑通后你就真的理解 IPC 了。
4. 进阶：搜索「Electron 安全 checklist」，了解为什么默认要关闭 `nodeIntegration`。

---

## Step 5（M1）：本地历史记录

**做了什么**

- 主进程把聊天记录以 JSON 文件形式保存在 Electron 的 `userData` 目录（`history.json`），即 Windows 下 `%APPDATA%\神里绫华 AI 陪伴\history.json`。
- 每次对话结束调用 `history:save` 写入；应用启动时调用 `history:load` 恢复，实现「重启可续聊」。
- 界面上提供「清空对话」按钮，对应一次 `history:save([])`。

**为什么**

- 纯本地优先：不建数据库、不做云同步，一个 JSON 文件就够 MVP 用。
- 存在 `userData` 而不是项目目录，符合桌面应用的惯例（用户数据与应用代码分离）。

**独立学习路径**

1. 了解 Node.js 的 `fs` 模块读写文件：<https://nodejs.org/api/fs.html>（先看 `readFileSync` / `writeFileSync`）。
2. 了解 JSON 格式和 `JSON.parse` / `JSON.stringify`。
3. 小练习：让历史记录按「天」拆成多个文件（如 `2026-08-12.json`），再写个读取最近三天的功能。
4. 进阶：把 JSON 换成 SQLite（搜 `better-sqlite3`），体会「什么时候才值得上数据库」。

---

## Step 6（M1）：接入大模型（OpenAI 兼容 API + 模拟模式）

**做了什么**

- `chat:send` 处理器把「system prompt（角色卡）+ 最近 40 条消息（约 20 轮）」发给大模型。
- 用 Node 内置的 `fetch` 调用 `/chat/completions` 接口，兼容 DeepSeek / Kimi / 通义 / OpenAI 等。
- 配置通过 `.env` 文件提供（`OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`），`.env` 已加入 `.gitignore`。
- **没配 Key 时自动进入模拟模式**：返回预设的、带人设口吻的回复，保证 MVP 在零成本下可完整跑通。

**为什么**

- 只保留最近 40 条消息是**成本控制**：上下文越长，每次调用越贵。
- `.env` + `.gitignore` 确保 Key 不硬编码、不进版本库。
- 模拟模式是练手项目的救命设计：没有 Key 也能开发、演示、学 UI。

**独立学习路径**

1. 学 `fetch` 和 async/await（MDN 教程）：<https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API>
2. 看 DeepSeek 的 API 文档（OpenAI 兼容格式的典型例子）：<https://api-docs.deepseek.com/>，重点看 `chat/completions` 的请求格式。
3. 用 Postman / curl 直接调一次 API，看返回 JSON 长什么样，再对照本项目的 `callOpenAICompatible` 函数。
4. 小练习：把 `max_tokens` 改小，观察回复被截断；加一个 `temperature` 调低，观察语气变「保守」。
5. 进阶：研究「上下文管理」——怎么压缩历史、怎么提炼长期记忆（这是 M2 的核心）。

---

## Step 7：构建、冒烟测试与踩坑记录

**做了什么**

- `npm run build`：验证前端能打包成 `dist/`。
- 冒烟测试：设置 `SMOKE_TEST=1` 启动 Electron，渲染进程加载成功会写一个标记文件然后自动退出，用这个机制自动化验证「应用真的能打开」。

**踩过的坑（这些经验比代码值钱）**

1. **Electron 二进制下载失败**：国内网络下从 GitHub 下载 Electron 会出现 TLS 证书校验错误。解决：`$env:ELECTRON_MIRROR='https://npmmirror.com/mirrors/electron/'; npm install`。
2. **PowerShell 启动 GUI 程序不等待**：`& electron .` 会立即返回，导致后面的检查跑得太早。解决：用 `Start-Process -Wait` 或轮询标记文件。
3. **中文输入法误发送**：按 Enter 选字会触发发送逻辑。解决：判断 `e.nativeEvent.isComposing`。
4. **API Key 不要放前端**：渲染进程是浏览器环境，Key 放进去等于公开；统一走主进程。

**独立学习路径**

- 学 `Start-Process`、环境变量、进程退出码这些「脚本自动化」技能，练手项目很多验证都能靠它们自动化。
- 养成习惯：每完成一个里程碑先跑通验证（build + smoke），再进入下一步，不要一次铺开全部功能。

---

## Step 8：换角色（把林离换成神里绫华）

**做了什么**

- 只改了两个地方：
  1. `src/shared/character.js`：整个角色卡换成神里绫华（稻妻社奉行神里家大小姐、白鹭公主，端庄优雅但熟悉后会露出俏皮一面，称呼用户为「旅行者」）。
  2. 把散落在代码和文档里写死的旧名字（标题、问候语、确认弹窗、头像字、窗口标题、README/日志）统一替换。
- 改完 `npm run build` 验证构建通过。

**为什么**

- 这是「角色卡集中管理」设计带来的红利：人设全部在一个对象里，换角色几乎不用动聊天逻辑。
- 但这次也暴露了一个小教训：**名字不能写死**。头像、标题、问候语里到处是「林离」，换角色时得逐个找。更优的做法是全部从 `character.name` 读取——这也是下一轮迭代值得顺手做的重构。

**独立学习路径**

1. 小练习：把 `App.jsx` 和 `ChatMessage.jsx` 里所有写死的名字/头像字都改成从 `character` 读取（头像已经改了，检查有没有漏的）。
2. 小练习：再换一个你喜欢的角色（比如随便编一个原创人设），数一数这次需要动几个文件——目标是最多只改 `character.js` 一个文件。
3. 思考题：如果以后想做「角色切换器」（下拉菜单换人设），现在的架构够用吗？还差什么？

---

## Step 9：加 few-shot 示例对话（让绫华更贴人设）

**做了什么**

- 在 `src/shared/character.js` 的角色卡里新增 `examples` 数组，放了 4 条「旅行者问、绫华答」的示例：问候、聊爱好、被问住、雨天。
- 新增 `buildFewShotMessages()`：把示例转成 user/assistant 交替的消息。
- `electron/main.js` 组装请求时，顺序变为：system prompt → few-shot 示例 → 最近历史。

**为什么**

- 描述性格是「抽象要求」，示例对话是「具体范本」。模型从例子里学语气和节奏，比自己脑补描述更准。
- 示例放在角色卡里，延续「人设集中管理」的设计；加几条、删几条都不碰聊天逻辑。

**独立学习路径**

1. 搜「LLM few-shot prompting」理解原理：模型如何从示例中推断输出格式和风格。
2. 小练习：自己写 3 条「希望她怎么回」的对话，观察改完前后的语气差异。
3. 对比练习：把示例数量从 4 条改成 0 条和 8 条，感受「稳定性」和「token 成本」之间的权衡——每条示例都会占一点上下文。

---

## Step 10（M2）：长期记忆——让绫华记住你

**做了什么**

- 新增 `memory.json`（`userData` 目录），存放「关于旅行者的记忆」列表：每条含 id、文本、创建/更新时间。
- 每条用户消息后，后台调用一次大模型做**记忆提取**：从对话中提炼关于用户的值得记住的事实（名字、喜好、约定……），输出 JSON。
- 提取结果与已有记忆**合并去重**（文本相同或互相包含就视为同一条，只刷新时间），最多保留 50 条，超出淘汰最久没更新的。
- 下次对话时，记忆以「关于旅行者，你记得这些事：……」的形式注入 system prompt。
- 界面头部新增「记忆 N」按钮，点开能看到绫华记住了什么；提取完成后界面自动刷新（主进程 → 渲染进程的**事件推送**，和之前 `invoke` 的请求-响应是 IPC 的两个方向）。
- 细节：清空对话时会重置提取计数，避免下轮新对话永远不触发提炼。

**为什么**

- 最初设计是「攒 4 条提炼一次」省钱，后来按你的要求改成**每条消息都提炼**，换取记忆的及时性——代价是每次对话多一次 API 调用（约 300 token，成本依然很低）。
- 加了防并发锁：提取进行中再来新消息会跳过本次，下一条消息自动补上，避免重复写文件。
- 模拟模式不触发；提取失败静默跳过，不影响聊天。
- 记忆和聊天记录分开存：聊天是流水，记忆是沉淀，互不干扰。
- 让用户能看到记忆，既直观又能检查提取质量——这是判断「记忆做得好不好」最简单的方式。

**独立学习路径**

1. 复习 Step 4 的 IPC，再看新的「事件推送」方向：主进程 `webContents.send` → 渲染进程 `ipcRenderer.on`。`invoke/handle` 是请求-响应，`send/on` 是主动通知——两个方向都掌握才算懂 IPC。
2. 学「信息提取/对话摘要」的提示词写法：指定角色、限定范围、规定 JSON 输出格式、给出反例（"不要提取关于角色的内容"）。
3. 小练习：把「每条都提炼」改回「攒 10 条提炼一次」，对比记忆及时性和 API 调用次数，体会成本与体验的取舍。
4. 思考题：现在的去重是「文本相似就合并」，太笨。「旅行者喜欢下雨天」和「旅行者雨天会心情好」明明是相关的，怎么合并成一条更完整的记忆？（提示：可以让模型输出「新增/修改/删除」三类操作，做记忆的二次整理。）

**踩坑记录（Step 10 特别版）**

- 调试时用 PowerShell 管道把「含中文的脚本」传给 `node -` 执行，中文在传输中被编码成问号，模型收到的全是乱码，导致测试结果完全失真（还误以为提示词写得不好）。
- 教训：**涉及中文的测试脚本要写成 UTF-8 文件再运行**，不要用管道内联。所以本项目把测试做成了正式脚本 `scripts/test-memory.mjs`，运行 `npm run test:memory` 即可验证整条记忆链路。

---

## Step 11：排查「清空对话后无法再次输入」的 bug

**现象**：清空一次对话后，输入框和发送按钮失效，无法再发消息。

**排查过程**

1. 读应用数据文件：`history.json` 已是 `[]`（清空成功了），但 `memory.json` 里一条记忆都没有——说明清空之前，记忆提取一次都没成功过。
2. 写自动化复现脚本 `scripts/repro-clear.mjs`（通过 Chrome DevTools 协议驱动真实应用：发消息 → 清空 → 再发消息），结果**流程完全正常，没有复现**。
3. 结合现场数据推断最可能的真实场景：**清空时上一条请求正卡着没返回**。原代码的 `fetch` 没有超时，一旦 DeepSeek 不响应，「正在输入」状态永远不结束，发送按钮永久禁用——清空虽然成功，但输入锁没解开。

**修复（两个真实隐患）**

- 所有模型请求加 **60 秒超时**（AbortController），卡住时自动中止并提示「请求超时」，输入锁必然能解开。
- 清空对话时：作废等待中的旧回复（代际标记 `generationRef`），防止迟到的回复把已清空的旧对话「复活」；同时主动解除输入锁定、关闭记忆面板、加错误提示。

**独立学习路径**

1. 学 `AbortController` 和 `AbortSignal`：<https://developer.mozilla.org/zh-CN/docs/Web/API/AbortController>——网络请求「可能永远不返回」是常识，生产代码必须设超时。
2. 理解「竞态条件」：异步操作完成时，外部状态可能已经变了（比如用户点了清空）。常见解法就是本项目的「代际标记」——每次清空 +1，旧请求回来时对比自己的编号，对不上就丢弃。
3. 小练习：把超时改成 3 秒，发一条消息看提示；再试试在回复到达前点「清空对话」，观察旧回复是否会被正确丢弃。

---

## Step 12：第二次排查——「清空后整个程序卡住/像重启了一样」+ 记忆丢失

**现象升级**：用户反馈更精确了——不是不能输入，而是清空后**整个程序卡住，像重启了一样**；而且之前说的名字、年龄没有被记住。

**证据**

- `history.json` 和 `memory.json` 都是空的：说明那次对话既没存历史，也没存记忆——应用在保存完成前就被中断了。
- Windows 事件日志里没有 Electron 崩溃记录：不是进程崩溃，更像是渲染进程被「冻结」。

**根因：`window.confirm` 是同步阻塞的原生弹窗，在 Electron 里会冻结渲染进程**

- 点击「清空对话」→ `window.confirm` 打开一个同步模态弹窗，渲染进程的主线程整个停住；
- 这个弹窗在某些 Windows 环境下还会出现在窗口**后面**，用户根本看不到它，只看到程序"卡死"；
- 用户以为是崩溃，直接关掉重开 → 正在后台进行的记忆提取（异步、还没写完文件）被一起杀掉 → 名字、年龄没存进 `memory.json`。

**修复**

1. **弃用 `window.confirm`**，改用主进程的 `dialog.showMessageBox`（异步、非阻塞、始终在最前）。清空确认通过 IPC `dialog:confirm-clear` 完成。
2. **启动自愈**：应用启动加载历史时，检查有没有「用户消息数 - 已提炼数 ≥ 1」的欠账，有就补一次记忆提取——即使上次应用在提取中途被关闭，下次打开也会补上。
3. 记忆提取失败时在控制台输出日志（`console.error`），不再完全静默，方便以后诊断。

**独立学习路径**

1. 记住这条经验：**Electron 里不要用 `window.confirm` / `window.alert`**，一律走主进程 `dialog` 模块 + IPC。这也是官方文档推荐的做法。
2. 理解「同步阻塞 vs 异步」：`window.confirm` 阻塞的是渲染进程的 JS 主线程；而 `dialog.showMessageBox` 返回 Promise，不阻塞。
3. 理解「自愈设计」：凡是「后台任务可能被打断」的流程（提取、上传、同步），都可以在启动时对账补跑——这是工程里很常用的一招。

---

## Step 13：记忆自相矛盾 + 清空确认弹窗改版

**现象**：记忆面板里同时出现三条互相冲突的结论：

```
• 用户叫郭成卓
• 用户没有告知自己的名字，需要询问
• 用户没有告诉过绫华自己的名字
```

**分析**：三条结论确实冲突。根因是**每次记忆提取都是独立的**——只看最近一段对话，看不到已有记忆，也没有对账机制。模型在一个片段里听到「我没告诉过你名字」就存了否定结论，在另一个片段里听到「我叫郭成卓」又存了新名字；代码里的去重只做字符串比较，看不出语义矛盾，三条就都留下了。

**修复 1：带上下文的记忆对账**

- 提取时把「目前记住的事实」一起发给模型，让模型输出 **add / remove** 两类操作：被推翻的旧结论先 remove，新信息再 add；
- 提示词里明确禁止把「用户没有告知XX」这类临时状态入库；
- 纯函数 `applyOps` 负责执行增删（仍是纯本地逻辑，可单独测试）。

**修复 2：清空确认弹窗改版**

- 弃用系统原生对话框（它无法定制样式、长得丑）；
- 改成 React 自研弹窗：毛玻璃遮罩 + 角色头像 + 主题配色 + 弹出动画，点遮罩或「取消」关闭。

**独立学习路径**

1. 这是 Step 10 思考题的现实版：记忆系统必须「对账」，否则不同时间提取的结论会打架。带已有记忆 + 让模型输出 add/remove 是常见解法。
2. 练习：给弹窗补上 Esc 键关闭（✅ 已做）、无障碍属性（✅ `role="dialog"`、`aria-modal` 已加）；焦点陷阱可以作为后续练习。
3. 对比：系统弹窗 vs 自研弹窗的取舍——自研好看可控，但要自己处理键盘焦点和无障碍。

---

## Step 14：记忆被误删事件——给记忆系统加「备份 + 保守删除」

**现象**：用户发现 `memory.json` 里的记忆被清空了（连「用户叫郭成卓」都没了）。

**排查**：文件格式是应用自己写入的，说明是应用内某次「记忆整理」把已有事实错误地 remove 掉了——对账机制给了模型删除权限，但模型在拿不准时可能误删。

**修复（三层加固）**

1. **写入前自动备份**：`saveMemory` 先把当前文件复制为 `memory.json.bak` 再覆盖——误删可恢复；
2. **提示词加保守规则**：只有最新对话**明确**给出相反/更新信息才允许 remove；对话自相矛盾时以更靠后的信息为准；拿不准就保留，「宁可多留、不可误删」；
3. **操作留日志**：每次整理完成，控制台输出 add/remove 操作，方便事后追溯是谁删的、删了什么。

**独立学习路径**

1. 思考题：给 LLM 系统「删除权限」前，先想清楚误删成本。记忆这类珍贵数据，默认应该保守（保留优先），删除必须来自明确证据。
2. 练习：写一个从 `memory.json.bak` 一键恢复的小脚本（或者做成应用内的「记忆管理」功能）。
3. 体会「可观测性」：没有日志时，误删只能靠猜；有了操作日志，一次就能定位。

---

## Step 15（M3）：活人感——随机延迟 + 主动发消息

**做了什么**

1. **随机「思考」延迟**：发送消息前先等 `600ms + 消息长度×25ms + 随机 0~1.2s`——短消息回得快、长消息回得慢，打字动画期间看起来像真的在想。
2. **主动发消息**：
   - 新 IPC `chat:proactive`：用「人设卡 + 记忆 + few-shot + 一条『由绫华主动开场』的指令」生成主动消息，不依赖用户输入；
   - 渲染进程加调度器：用户安静 45 秒后绫华开口；发完一条后 3 分钟内不再主动打扰；
   - 防打扰三件套：用户正在打字/等待回复时跳过、窗口没有聚焦就推迟 30 秒、清空对话时用代际标记作废正在生成的主动消息。

**为什么**

- 「活人感」的本质是让交互节奏像人：回复太快显得机器、太规律显得假；偶尔主动开口才像朋友。
- 主动消息的生成也走人设卡 + 记忆，所以她会聊到记得的事（比如叫出你的名字），而不是随机废话。

**独立学习路径**

1. 学 `setInterval` + ref 的「调度器」模式：用 ref 存"下次触发时间"，定时器只做检查，避免频繁重建定时器。
2. 思考「防打扰」：主动通知类功能要考虑窗口聚焦、用户忙碌、频率上限——这是产品设计题，不只是代码题。
3. 练习：把空闲时间（45 秒）和冷却时间（3 分钟）做成可配置（设置面板或 `.env`）；给主动消息加一个开关按钮。

---

## Step 16：记忆第三次被误删——治本方案

**现象**：加了「保守删除」提示词后，已恢复的「用户叫郭成卓」还是被清掉了。

**根因**：自愈补提炼和每次提取都把**整段历史**交给模型重新审判。旧对话里「用户没告知名字」这类矛盾表述会被反复翻出来，模型每次都可能据此误删正确的事实——提示词再保守也挡不住。

**治本修复（三层）**

1. **只整理新消息**：`unprocessedMessages()` 按「已处理用户消息数」截取上次整理之后的对话，只把增量交给模型——旧历史不再被反复审判，矛盾源头直接切断；
2. **核心信息保护锁**：记忆条目加 `protected` 标记，`applyOps` 对受保护条目一律不执行删除；已确认的名字/年龄这类核心信息从此只有人工能改；
3. **提示词配合**：明确告知模型「已确认的核心信息以已有事实为准，不要 add 冲突条目」。

**独立学习路径**

1. 思考「增量 vs 全量」：让 LLM 重新评估全部历史看似更全面，但每次都会引入旧错误的扰动；增量处理 + 保护关键数据往往更稳。
2. 练习：实现「记忆管理」界面——展示受保护条目，允许用户手动解锁/修改。
3. 理解取舍：保护锁让名字不会被模型改错，代价是用户真改名字时也需要手动更新——安全优先时这是合理选择。

**后续真相（第 17 步的前传）**：折腾一轮后发现，「误删」其实还有第三个元凶——恢复脚本用 PowerShell 写入 UTF-8 **带 BOM** 的文件，Node 的 `JSON.parse` 遇 BOM 直接抛错，应用读不到恢复的记忆，回退为空再重新整理。保护锁本身一直是对的。应用已加 BOM 防御。

---

## Step 17（M3 补完）：打字机逐字显示

**做了什么**

- 回复不再一次性出现：API 返回完整文本后，界面按「约 28~46ms/字」的随机速度逐字显示，末尾带一个闪烁光标；
- 历史记录仍保存**完整**文本，重启后直接显示全文，不做动画；
- 动画期间输入框保持锁定（她还在"打字"）；清空对话会中断动画；主动消息同样走逐字效果。

**为什么**

- 逐字输出是「活人感」的重要一环：一次性吐完一段话很机器，逐字出现会让人产生"她正在打字"的错觉。
- 展示层动画、数据层存全文——「显示效果」和「真实数据」分离，历史、记忆、导出都不受动画影响。

**独立学习路径**

1. 学「递归 setTimeout」实现打字机：`setInterval` 固定间隔会丢节奏，递归 `setTimeout` 可以每次随机间隔、按需提前结束。
2. 思考「动画状态 vs 数据状态」的分离：动画只是 `reveal.count` 这一个小状态，消息内容始终是完整的。
3. 练习：加「点击跳过动画」——点击正在播放的消息直接显示全文；或把速度做成可调节的滑块。

---

## Step 18（M4）：语音——Edge TTS 朗读 + Whisper 语音输入

**方案取舍（灵魂优先）**

- 最初想用 Web Speech API 的 `speechSynthesis`（零依赖、零 Key），但 Windows 自带中文语音（慧慧）非常机器化，用户直接否决：「完全失去了绫华本身的灵魂」。结论：**TTS 的音色就是角色的灵魂，不能用难听的声音凑合**。
- 改用 **Edge TTS**（微软神经语音，免费、无需 Key，`zh-CN-XiaoxiaoNeural` 晓晓），音质自然很多。将来想还原绫华原声，需要 GPT-SoVITS / fish-speech 这类音色克隆方案（要音频素材 + 本地训练，且涉及版权，列为后续课题）。

**实现**

- 主进程 `tts:speak`：`MsEdgeTTS.toFile()` 合成 MP3 → 读文件转 base64 → 渲染进程用 `Audio(dataURL)` 播放；失败时兜底回退到系统语音。
- 界面：每条绫华消息带「🔊 朗读」按钮；头部「朗读 开/关」开关，开启后回复逐字播完自动朗读（和打字机效果衔接）。
- 语音输入 `stt:transcribe`：渲染进程 `MediaRecorder` 录音（webm）→ 主进程调 OpenAI 兼容 `/audio/transcriptions` → 文字填入输入框。主进程配置了麦克风权限。

**踩坑：toStream 36 秒 vs toFile 1.2 秒**

- 用 `toStream()` 收音频时，音频首块 0.8 秒就到，但连接关闭前空等 30+ 秒（`for await` 一直不结束）；
- 换 `toFile()` 后 1.2 秒完成——**测库之前先读对 API，选对方法差 30 倍**。

**独立学习路径**

1. 学 Web Audio / `Audio` API：`data:` URL 播放、播放中断（`pause()`）、以及为什么 base64 直接嵌在 URL 里最简单（文件清理都不用做）。
2. 学 MediaRecorder：`getUserMedia` 拿麦克风流、`ondataavailable` 攒块、`Blob` 打包；注意 Electron 里要配 `setPermissionRequestHandler` 放行 `media` 权限。
3. 思考产品问题：自动朗读默认开还是关？播放中被新消息打断怎么处理？这些交互细节决定「像不像真人在说话」。

**教训补充**：自动化测试脚本会真实操作应用数据——复现流程里的「清空对话」把用户当时的一段聊天记录清掉了。给 `history.json` 也加了和记忆一样的「先备份再覆盖」机制（`history.json.bak`），以后出问题可恢复。

---

## Step 19（M4 进阶）：音色克隆接入点

用户想要绫华原声。结论：

- 硬件（RTX 3050 4GB + 16GB 内存）可跑 GPT-SoVITS 零样本克隆，无需训练即可用；
- 版权提醒：克隆角色原声只适合私人练手，不传播、不商用；
- 应用侧：`tts:speak` 增加 `CLONE_TTS_URL` 优先通道（GET 风格，返回 WAV），克隆服务不可用时自动回退 Edge TTS；
- 写了 [音色克隆指南.md](../音色克隆指南.md)，从环境准备到接入应用一步步可照做。

**独立学习路径**

1. 了解零样本克隆与微调的区别：零样本 = 参考音频现学现用；微调 = 用更多数据把模型调成「专属音色」，更稳更贴。
2. 思考集成架构：主进程里「多音色通道 + 自动回退」让应用永远不因为某个 TTS 挂了而坏掉。

**实战排障记录（音色服务跑通）**：

- 12 分钟音频 → 切片 140 段 → SenseVoice 自动转写 → 人工校对全文；
- 三个环境坑：torchcodec（需 FFmpeg DLL → 代码回退 soundfile）、fast_langdetect 缓存目录缺失、G2PW 解压多套一层目录；
- 应用侧：克隆服务不可用时现在会在界面上提示「正在使用备用语音」，不再无声回退；
- 服务必须保持运行，一键启动 bat 会自动拉起并等端口就绪。

**Step 20 速记：TTS 朗读不该念「舞台说明」**

- 问题：绫华的回复常带（指尖轻划）、【内心：无奈】这类动作/环境描写，TTS 会原样念出来，非常出戏。
- 修复：新增 `src/shared/ttsFilter.js`，朗读前过滤（）、【】、半角 ()、`*斜体动作*` 里的内容；渲染进程和主进程都做（双层防御），纯描写句子兜底读原文。
- 学习点：**「显示文本」和「朗读文本」是两件事**——展示给眼睛的内容可以带舞台说明，喂给耳朵的必须只含台词。这在有声书/语音助手产品里是常见处理。

**路线变更：M5 音乐砍掉**——用户明确不需要（内置钢琴曲、MIDI 上传与可视化），README 路线图已同步。剩余里程碑：M6 桌面感、M7 书信系统。

**Step 21 速记：语音输入配通（硅基流动 SenseVoice）**

- 语音转写选了国内可直连的硅基流动，模型 `FunAudioLLM/SenseVoiceSmall`，实测免费额度可用（无需充值）；
- 测试脚本 `npm run test:stt` 用参考音频验证整条链路（录音 → `/audio/transcriptions` → 文字填入输入框）；
- SenseVoice 转写结果常带 emoji，已在前端去掉；
- 换服务商只改 `.env` 里的 `STT_BASE_URL` / `STT_MODEL`（Groq 的 whisper-large-v3 也可）。

---

## Step 22（M6）：桌面感——托盘、悬浮球、开机自启

**做了什么**

- **系统托盘**：`Tray` + 右键菜单（显示/隐藏主窗口、悬浮球开关、开机自启开关、退出），程序化生成紫色"绫"字图标；
- **透明悬浮球**：无边框透明置顶小窗（84×84），点击唤起主窗口、按住拖动换位置；Vite 改为多页面（`index.html` + `floating.html`）；
- **关闭最小化到托盘**：关主窗口不退出，托盘"退出"才真正退出；`before-quit` 处理系统关机；
- **开机自启**：`app.setLoginItemSettings`，托盘菜单开关；
- **单实例锁**：重复启动时唤起已有窗口，防止开两个。

**踩坑**

- 无边框窗口的 `-webkit-app-region: drag` 会吞掉点击事件 → 改用 JS 手动拖动（mousedown/mousemove + `setPosition`）；
- 冒烟测试 `app.quit()` 被 close-to-tray 拦截导致永远退不出 → 退出前先置 `isQuitting = true`（也提醒：关窗口≠退出，托盘退出才是真退出）。
- 悬浮球"一碰就飞走"：拖拽把「起点差值」反复叠加到「当前位置」导致加速。修复：按下时记录窗口起点，移动时用**绝对定位（起点+位移）**。

**独立学习路径**

1. Electron 官方文档：Tray、frameless window、`setLoginItemSettings`；
2. 理解"关闭行为"设计：关窗口是隐藏还是退出，是桌面应用最影响用户体验的决策之一；
3. 练习：记住悬浮球位置（localStorage）、双击悬浮球切换主窗口显示/隐藏。

---

## 下一步（M2 记忆系统）的学习预告

M2 要做「长期记忆」：对话结束后从内容里提炼用户的重要信息（名字、喜好、约定），存到本地，下次对话时注入上下文。

建议现在开始准备：

- 学 `fs` 追加/更新 JSON 文件的模式。
- 思考「怎么从一段对话里提炼事实」——这是提示词工程的一个经典场景（可以搜「LLM conversation summarization」）。
- 想清楚「记忆注入到哪里」：可以像角色卡一样作为 system prompt 的一部分，也可以作为独立消息插入。

---

## 推荐学习资源汇总

| 主题 | 资源 |
| --- | --- |
| React | <https://zh-hans.react.dev/learn> |
| Vite | <https://cn.vitejs.dev/guide/> |
| Electron 进程模型 | <https://www.electronjs.org/docs/latest/tutorial/process-model> |
| Electron 预加载/安全 | <https://www.electronjs.org/docs/latest/tutorial/tutorial-preload> |
| Node.js fs | <https://nodejs.org/api/fs.html> |
| fetch / async | <https://developer.mozilla.org/zh-CN/docs/Web/API/Fetch_API> |
| DeepSeek API（OpenAI 兼容示例） | <https://api-docs.deepseek.com/> |
| Flexbox 布局 | <https://developer.mozilla.org/zh-CN/docs/Learn/CSS/CSS_layout/Flexbox> |

---

## 当前进度

- [x] M0 骨架：Electron + Vite + React 空窗口可运行
- [x] M1 角色聊天 MVP：角色卡 + 聊天界面 + 本地历史 + 大模型接入（含模拟模式）
- [x] M2 长期记忆：自动提炼并记住关于你的事，跨会话注入，界面可查看
- [ ] M3 活人感（打字动画已有雏形，待完善：时段问候已做、随机延迟、主动消息）
- [ ] M4 语音
- [ ] M5 音乐
- [ ] M6 桌面感
- [ ] M7 书信系统
