# 音色克隆指南：让绫华用「自己的声音」说话

> 请确保你使用的音频素材拥有合法使用权，本工具仅用于本地设备上的个人体验。

## 你的硬件够吗？

- NVIDIA GeForce RTX 3050 Laptop（4GB 显存）+ 16GB 内存 → **够用**。
- 零样本克隆（不训练）可以立刻跑；微调也能跑，只是慢一些（小数据集约 1~3 小时）。

## 推荐路线

- **方案 A：GPT-SoVITS 本地（推荐）**——免费、开源、中文效果最好、数据不出本机。
- **方案 B：fish.audio 等在线服务**——省事，但要把音频传云端，有免费额度限制。

---

## 方案 A：GPT-SoVITS 本地零样本克隆（最快）

### 第 1 步：准备环境

1. 安装 [Python 3.10](https://www.python.org/downloads/)（安装时勾选 Add to PATH）；
2. 安装 [Git](https://git-scm.com/download/win)；
3. 确认 NVIDIA 驱动已装（设备管理器里能看到 RTX 3050 即可，一般自带 CUDA 运行时）。

### 第 2 步：下载 GPT-SoVITS

```bash
git clone https://github.com/RVC-Boss/GPT-SoVITS.git
cd GPT-SoVITS
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

国内网络拉 GitHub 慢的话，可以用镜像（如 ghproxy）或直接下 Release 压缩包。

### 第 3 步：下载预训练模型

按项目 README 里的下载链接（通常百度网盘 / HuggingFace）下载，放到对应目录：

- `GPT-SoVITS-v2` 相关模型 → `GPT_weights_v2/`
- SoVITS 预训练模型 → `SoVITS_weights_v2/`

缺模型文件时程序会直接报错，这一步不能省。

### 第 4 步：准备绫华的参考音频（关键）

- 从游戏语音/粉丝存档里收集 **1~3 分钟干净人声**：无 BGM、无环境音、无回声；
- 切成 5~20 秒的片段（可用剪映/ Audacity），每个片段记住**对应的原文文字**（之后要填 `prompt_text`）；
- 质量比数量重要：一段 20 秒、人声干净的片段，比一小时嘈杂音频好得多。

### 第 5 步：启动 API 服务（零样本）

```bash
python api_v2.py
```

默认监听 `127.0.0.1:9880`（v2 轻量 API）。首次启动会要你填/确认：

- `ref_audio_path`：参考音频路径（如 `D:\ayaka\ref.wav`）；
- `prompt_text`：这段参考音频说的原文；
- `prompt_lang`：填 `zh`。

也可以用 URL 参数每次指定（见第 6 步示例）。

### 第 6 步：接入本项目

编辑 `G:\AI-girlfriendplan\.env`，加上：

```env
CLONE_TTS_URL=http://127.0.0.1:9880/tts?ref_audio_path=D%3A%5Cayaka%5Cref.wav&prompt_text=参考音频的原文
```

（路径里的 `\` 要编码成 `%5C`，`:` 编码成 `%3A`；也可以直接在 `api.py` 里填默认值，URL 就不用带参数。）

重启应用后：

- 朗读优先走绫华克隆音色；
- 克隆服务没启动/报错时，**自动回退到晓晓**，不会坏掉；
- 想看当前用的哪种，控制台会打印克隆失败日志。

### 第 7 步（可选）：微调，让声音更稳更像

跑 `python webui.py`，按界面顺序：

1. **训练音频切割**：把 5~15 分钟素材喂进去自动切片；
2. **SoVITS 训练**：微调 SoVITS 模型（你的显卡约 1~2 小时）；
3. **GPT 训练**：微调 GPT 模型（语速语气更像）；
4. **合成测试**：满意后，`api.py` 自动使用微调好的模型。

---

## 方案 B：在线服务（备选）

- [fish.audio](https://fish.audio)：上传参考音频 → 创建音色 → 拿 API 凭证；
- [Reecho](https://www.reecho.ai)：类似，支持中文；
- 注意：音频会上传第三方服务器，介意隐私就别用。

拿到在线接口后，把 `CLONE_TTS_URL` 换成它的合成地址即可（需要 HTTPS、OpenAI 兼容或 REST，具体看各家文档）。

---

## 常见问题

- **api.py 启动报错**：多半是依赖没装全或预训练模型没放对目录，看报错路径修复。
- **合成慢**：RTX 3050 下一句话 1~3 秒正常；嫌慢可调低采样/批次参数。
- **声音不像**：优先换更干净的参考音频；零样本不够像就走微调。
- **Windows 中文路径**：`api.py` 的参考音频路径尽量用英文路径，避免编码问题。
- **Python 3.12 兼容**：本仓库已做两处小补丁——`jieba_fast` 回退到纯 Python 版 `jieba`（三个文件），`opencc` 换成纯 Python 版；日/韩/粤语包在 `requirements-cn.txt` 中被跳过（中文合成不受影响）。
