# 复制本文件为 .env，填入真实密钥后重启应用即可接入真实模型。
# 不配置时应用自动进入「本地模拟模式」，界面照常可用。
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat

# 语音输入（可选）：需要一个支持 /audio/transcriptions 的 Whisper 兼容接口。
# DeepSeek 不支持语音转写；可用 OpenAI（whisper-1）或 Groq（whisper-large-v3）等。
# 不配置时「语音输入」按钮会给出提示，其余功能不受影响。
STT_API_KEY=
STT_BASE_URL=https://api.openai.com/v1
STT_MODEL=whisper-1

# 朗读语音（TTS）：Edge TTS 神经语音，免费无需 Key。默认晓晓，可选 zh-CN-XiaoyiNeural 等
TTS_VOICE=zh-CN-XiaoxiaoNeural

# 音色克隆（可选）：本地 GPT-SoVITS 等服务地址。配置后朗读优先用克隆音色，失败自动回退 Edge TTS。
# 示例（GPT-SoVITS api.py 默认端口）：
# CLONE_TTS_URL=http://127.0.0.1:9880/tts?ref_audio_path=D%3A%5Cayaka%5Cref.wav&prompt_text=参考音频的原文
# CLONE_TTS_URL=
